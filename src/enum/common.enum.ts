export enum Gender {
    MALE = 'M',
    FEMALE = 'F'
}

export enum GenderValue {
    MALE = 'male',
    FEMALE = 'female'
}

export enum ResponseMessage {
    COMPLETED = 'COMPLETED'
}

export enum ResponseCode {
    SUCCESS = 0,
    FAILED = -1
}